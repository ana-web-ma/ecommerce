import React, { type ReactElement } from 'react';
import { TableCell, TableRow } from '@mui/material';

export default function CartLineItem(): ReactElement {
  return (
    <>
      <TableRow>
        <TableCell>CartLineItem</TableCell>
        <TableCell>CartLineItem</TableCell>
        <TableCell>CartLineItem</TableCell>
      </TableRow>
    </>
  );
}
